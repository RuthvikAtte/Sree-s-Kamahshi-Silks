rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /saree_images/{allPaths=**} {
      allow read: if true;
      // Relaxed for local development/emulator. Revert to admin-only for production.
      allow write: if true;
    }
  }
}


